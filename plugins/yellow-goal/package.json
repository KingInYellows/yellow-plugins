{
  "name": "yellow-goal",
  "version": "0.2.0",
  "private": true,
  "description": "yellow-goal engine bridge — spawn goal-gen as a process (read-only requests plus stub-only runs), never import it",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --dir tests"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=22.22.0 <25.0.0"
  }
}
