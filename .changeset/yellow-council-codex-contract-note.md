---
"yellow-council": patch
---

Note in `council-patterns` SKILL.md that `codex-reviewer` now emits the
same structured 6-key contract as the Gemini and OpenCode reviewer slots,
for symmetry with those subsections. No functional change to
`council.md` — its `parse_reviewer_return` was already reviewer-agnostic.
