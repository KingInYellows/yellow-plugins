---
"yellow-linear": patch
---

Redact Linear MCP responses immediately after fetch, including cycle-path `list_cycles`/`list_issues` in `/linear:work` Step 1, before session display and worktree writes.
