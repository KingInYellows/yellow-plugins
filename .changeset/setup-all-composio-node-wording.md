---
"yellow-core": patch
---

`/setup:all`: clarify the yellow-composio PARTIAL classification so a
missing `node` binary is called out with its correct remediation (install
Node.js 18+) instead of being folded into the "restart Claude Code" hint — a
restart or disable/enable cannot fix a missing binary. READY / NEEDS SETUP
logic is unchanged.
