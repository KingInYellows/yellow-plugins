---
"yellow-ci": patch
---

Reword CLAUDE.md's PR-creation convention to point at the resolved active
stacked-PR provider (`/stack:status`) instead of naming `gt submit`
specifically. No functional change.
