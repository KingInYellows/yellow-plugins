---
'yellow-review': patch
---

Add the internal review-findings ledger library (`lib/review-ledger.sh`), its
rule vocabulary and a Bats suite. yellow-review now declares a required
yellow-core dependency for credential redaction. No command uses the ledger yet.
