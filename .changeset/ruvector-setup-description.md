---
"yellow-ruvector": patch
---

fix: collapse the /ruvector:setup description from a wrapped multi-line single-quoted scalar to a single line — Claude Code's frontmatter parser silently truncates multi-line description values, which can blank the command's marketplace listing
