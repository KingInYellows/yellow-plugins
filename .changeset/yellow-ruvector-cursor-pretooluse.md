---
"yellow-ruvector": patch
---

Stop Cursor from blocking Shell and file edits when leftover `ruvector hooks init` PreToolUse commands print empty stdout. Plugin hooks now emit dual-client allow JSON, setup creates `.ruvector/` without `hooks init`, and a repair script wraps existing empty-stdout PreToolUse entries.
