---
"yellow-linear": patch
---

docs: correct both CLAUDE.md restatements of the `/linear:delegate` dependency
— the command validates the `DEVIN_SERVICE_USER_TOKEN`/`DEVIN_ORG_ID`
environment variables (delegate.md Step 1), not yellow-devin plugin presence;
installing yellow-devin is one way to obtain the credentials, not a hard
plugin dependency.
