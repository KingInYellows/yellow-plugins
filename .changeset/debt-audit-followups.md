---
"yellow-debt": patch
---

fix: replace stale Linear MCP tool name in /debt:sync (`create_issue` → `save_issue` with `team`/`project`/`labels` params); restore the missing file-scope-validation bash block in debt-fixer Step 3 (was a dangling "see original agent" stub); use canonical 3-segment `yellow-debt:remediation:debt-fixer` subagent reference in /debt:fix
