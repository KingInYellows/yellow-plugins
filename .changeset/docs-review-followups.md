---
"yellow-docs": patch
---

fix: align /docs:review adversarial depth derivation with the agent's first-match-wins tier predicates (the old text could pass depth="quick" for documents the agent's own Quick predicate rejects); rewrite the command description to lead with when-to-use trigger phrases and name the commands to use instead for out-of-scope requests
